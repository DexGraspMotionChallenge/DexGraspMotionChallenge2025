# DexGrasp Task 2 实验方案

本文档记录正式实验设计和理由；实际操作及结果仍简要记录在 `PROJECT_LOG.md`。方案变更时直接更新本文档，不修改官方示例代码来实现自定义方法。

## 1. 目标与约束

目标是在一周内训练一个多物体共享抓取策略，优先提高选定训练物体的官方成功率，并额外验证对未参与训练物体的泛化能力。

- 本地设备：RTX 4060 Laptop 8 GB，必须能够完成核心实验。
- 可借服务器只作为后期加速，不作为方案成立的前提；本地验证有效后才考虑长训练。
- 官方成功实现保持不变。任务目标是抬升到桌面上方 30 cm；实际代码将“距该目标不超过 12 cm，或达到目标高度”记为成功。自定义 reward 和严格相对抬升 30 cm 统计不能替代这个官方指标。
- 官方代码作为可复现基准保留；新增环境、模型、配置和训练入口放入独立文件。

## 2. 数据范围

选择四个几何差异明显且原始数据充足的类别：

| 类别 | 作用 |
| --- | --- |
| bottle | 细长、近似圆柱，代表包握 |
| mug | 带把手且不对称 |
| bowl | 宽而浅，需要抓边缘或侧面 |
| camera | 块状、有凸起，形状不规则 |

每类选择 4 个训练物体和 1 个未见测试物体，共 16 个训练物体、4 个测试物体。训练物体覆盖简单、中等和困难形状；所有物体必须先通过 mesh、轨迹、Isaac Gym 加载和官方预处理检查。测试物体在训练前固定，不能根据模型表现重新挑选。

最终物体清单固定在 `custom_tools/configs/object_split_final.json`。两个官方保留轨迹过少的 bowl 训练候选按预先规定的同几何组备选规则替换；测试物体未按模型表现调整。

两个现有瓶子只用于验证数据、训练、仿真、评价和渲染链路，不代表正式实验范围。

## 3. 方法来源与定位

主方法为：

> 多物体 BC 基础策略 + 冻结基础策略 + 单一共享有界残差 PPO。

该思路借鉴以下工作：

- [Residual Reinforcement Learning for Robot Control](https://arxiv.org/abs/1812.03201)：在已有控制器上叠加 RL 修正。
- [Residual Reinforcement Learning from Demonstrations](https://arxiv.org/abs/2106.08050)：以示范策略作为基础策略，在 28 自由度灵巧手等任务上学习残差。
- [DAPG](https://arxiv.org/abs/1709.10087)：使用示范降低高自由度灵巧手 RL 的探索成本。
- [DexRepNet](https://arxiv.org/abs/2303.09806)：证明 DexRep、深度强化学习与多物体泛化的组合具有任务相关性。

我们不声称发明残差强化学习。项目贡献定位为：将基于示范的有界残差 RL 适配到 DexGrasp 多物体任务，并研究分组残差、阶段 reward、难度课程和未见物体泛化。

## 4. 基础 BC 策略

训练两种共享 BC，并在相同条件下比较：

1. 从官方单物体 checkpoint 开始进行多物体微调；
2. 在 16 个训练物体上随机初始化并从头训练。

四个类别平衡采样，不能让数量较多的 bottle 主导训练。根据训练物体固定验证种子上的官方成功率选择更好的 BC，随后冻结其参数，作为残差 PPO 的基础策略。

BC 的作用是学习靠近、调整姿态、闭合手指和抬升等主体动作；PPO 只负责修正执行误差。

执行结果（2026-07-18）：两组各训练 100 epoch、21,600 次更新。在训练物体每个 10 次的固定评测中，warm-start 与 scratch 总成功率均为 7.5%；warm-start 在 bottle、mug、bowl、camera 四类均有成功，scratch 的 camera 为 0%。因此共享残差 PPO 暂以 `multicategory_bc_warmstart_seed2025_e100/last.ckpt` 为基础策略，scratch 保留为对照。未见测试物体未参与该选择。

闭环鲁棒BC结果（2026-07-19）：在完全相同的warm-start、数据、类别采样、seed和100 epoch下，只对100维本体观测加入均匀噪声±0.02。训练约141秒，峰值预留显存98 MiB。虽然验证loss在epoch6后由0.0321逐渐升至0.0461，但epoch100闭环固定集由原BC的13/160提升至25/160，分类别 bottle/mug/bowl/camera 为7/3/11/4；独立100条验证轨迹由2/100提升至16/100，且高于残差PPO的8/100。配对固定集保留5、新增20、丢失8，平均最大抬升5.18→8.82 cm。闭环动作MAE下降7.9%，其中手腕位置误差下降约31%，但教师状态MAE略有增加。结论是小噪声以少量离线精度换取了显著闭环鲁棒性，epoch100噪声BC成为当前最佳基础策略；未见物体仍保持冻结。

## 5. 共享残差 PPO

最终动作定义为：

\[
a_t=\operatorname{clip}\left(a_{BC}(s_t)+\alpha\tanh(a_{res}(s_t))\right)
\]

- `a_BC`：冻结 BC 输出；
- `a_res`：共享 PPO Actor 输出的残差；
- `alpha`：残差幅度上限；
- `clip`：保证最终动作不越界。

手腕和手指使用不同残差上限：手腕修正更小，手指修正可以稍大。训练初期上限很小，策略稳定后再逐渐放宽，减少随机探索破坏基础动作的风险。

所有 16 个物体共用一个残差 Actor，不输入物体编号，依靠 DexRep 表达手与物体的几何关系。

### Actor 与 Critic

- Actor 输入：DexRep、手部状态、BC 动作和短期动作/状态历史。
- Critic 输入：Actor 的全部信息，加上训练阶段可获得的物体位置、速度、接触和高度等仿真完整状态。
- 评价时只保留 Actor，Critic 的特权信息不会成为部署依赖。

默认使用最近 3 帧历史。当前控制频率为 20 Hz，因此 3 帧覆盖约 0.1 s。正式长训练前短测 1、3、5 帧，分别对应无历史、约 0.1 s 和约 0.2 s，再根据成功率、速度和显存选择。

短测结果（2026-07-18）：修复局部重置稳定期后，三组各训练 50 轮；1/3/5 帧在 28 条同物体留出轨迹上的官方成功分别为 0/28、3/28、0/28，平均最大抬升为 0.80/10.92/1.06 cm。纯 BC 零残差为 0/28、平均最大抬升约 0.95 cm，因此 3 帧获得真实改善并进入下一阶段；单 seed 结果只用于工程选型，不作为统计显著性结论。

## 6. Reward 原则

训练 reward 使用阶段性进步信号：

\[
r_t = w_1\Delta d_{approach} + w_2 r_{contact}
      + w_3\Delta h + w_4 r_{30cm}
      - p_{drop} - p_{fly} - p_{jerk} - p_{residual}
\]

- 接近奖励：手掌和指尖与物体距离正在减小；
- 接触奖励：多根手指形成有效接触；
- 抬升奖励：物体高度正在增加；
- 成功奖励：官方成功标志首次成立时给予大额奖励；
- 惩罚：掉落、飞出工作区、动作抖动和不必要的大残差。

优先奖励“变化量”而非持续状态，避免策略停在物体旁边或维持无效接触来反复获得奖励。具体权重通过短实验调整，但官方成功判据不随 reward 变化。

第一阶段终止规则（2026-07-18）：官方成功标志首次成立时给予成功奖励并结束该 episode，以先建立稳定的挑战指标成功率。后续阶段在此基础上取消立即结束，继续奖励严格相对抬升 30 cm。官方成功率和严格 30 cm 达成率始终分别记录，不能互相替代。

第一阶段初始 reward 权重（2026-07-18）：接近进步 10、每新增接触 0.25、抬升进步 40，2/5/10/20 cm 里程碑各 0.5，官方成功 +10，掉落或飞出 -10；归一化残差幅度及相邻残差变化的平方均值各乘 -0.01。平滑项只约束 PPO 残差，不惩罚 BC 正常动作变化。

每个 reward 分项必须记录每步均值、标准差、绝对贡献比例和 episode 累计分布，并按类别拆分。零残差标定中各项绝对贡献为：接近 49.2%、抬升 26.0%、成功 10.3%、接触 6.2%、失败 5.2%、里程碑 3.1%，无单项超过 60%。混合环境基础成功为 2/16；各类别累计 reward 差异较大，因此采用分类别 advantage 标准化。

类别公平处理（2026-07-18）：对每类 advantage 分别去均值并除以 `max(std, 1.0)`，随后裁剪到 `[-5, 5]`。类别标准差小于 1 时只去均值、不放大噪声；Actor 不输入类别标签。CPU 极端尺度测试和 16 环境 Isaac Gym smoke 均通过，训练 CSV 保存标准化前后统计。

3 帧 50 轮中，各 reward 项平均绝对贡献为：接近 65.7%、失败 19.2%、抬升 11.2%、平滑 1.6%、接触 1.2%、残差幅度 0.9%、里程碑 0.1%、成功奖励 0%。对应平均单步标准差依次约为 0.0918、0.3072、0.0472、0.0003、0.0126、0.0002、0.0023、0。接近项在实际训练分布中超过 60%，后续必须监控；训练使用带噪动作而验证使用确定性均值，因此训练 rollout 没触发成功奖励，但验证得到 3 次成功。

正式 300 轮结果（2026-07-18）：每 50 轮验证一次，成功数依次为 1、0、1、0、0、0（共 28 条）；按预定规则选出的 `best.pt` 位于第 150 轮，成功 1/28，纯 BC 为 0/28，但低于 50 轮短测的 3/28。训练中接近项占绝对 reward 的 63.1%，成功奖励仅占 0.04%；reward 移动平均总体改善时，验证成功率反而在第 150 轮后归零，说明当前 reward 与最终成功存在偏差。下一步先做单变量诊断，不扩大到 12 或 16 个物体。

探索噪声消融（2026-07-18）：保持其他设置不变，将 `init_std` 从 0.25 降到 0.15，训练 150 轮。第 50/100/150 轮均为 0/28，平均最大抬升从 0.84 cm 降到 0.54 cm；同期训练 reward 的前/后 50 轮均值由 -0.0466 改善到 -0.0087，失败率由 9.38% 降到 6.25%。原噪声 0.25 在相同三个验证点分别为 1/0/1 次成功，最佳抬升 3.23 cm。因此较小噪声只让动作更保守，未提高抓取；当前主要矛盾进一步指向稠密 reward 与官方成功目标不对齐。

接近奖励消融（2026-07-18）：恢复 `init_std=0.25`，只将接近进步权重从 10 降到 5。第 50/100/150 轮成功数为 2/0/1，抬升为 4.20/1.95/2.05 cm；`best.pt` 位于第 50 轮，在 mug 和 bowl 各成功 1 条。原权重相同验证点为 1/0/1，最佳抬升 3.23 cm。接近项绝对贡献由约 63% 降到 48.5%，抬升项提高到 24.7%，训练成功事件由 1 次增加到 3 次；无动作饱和，最大 KL 0.0173。该单 seed 结果支持“降低接近 reward 捷径”，后续以权重 5 为新起点，但仍需更多 seed 或更大评估集确认。

抬升权重消融（2026-07-18）：在接近权重 5 的基础上，只将 `lift_progress` 从 40 增到 80。第 50/100/150 轮成功数为 1/0/0，抬升为 1.44/0.28/0.95 cm，低于权重 40 的 2/0/1 和 4.20/1.95/2.05 cm。抬升项每步标准差由约 0.0574 增至 0.1203，均值变为 -0.0080；最大 KL 0.0146、无动作饱和，说明不是 PPO 发散，而是有符号高度变化被整体放大后，下降惩罚使策略更保守。后续恢复 `lift_progress=40`。

历史最高高度奖励消融（2026-07-18）：新增可配置的 `max_height_progress`，只在刷新 episode 最高高度时奖励；下降和回到旧高度均为 0。CPU 测试证明不能通过上下抖动重复刷分，16 环境零残差标定为成功 2/16、失败 1/16，分解误差 1.43e-6。150 轮验证成功数为 0/0/1，抬升为 0.96/0.78/2.53 cm，最终成功来自 camera；但抬升项在训练分布中仅占 4.3%，接近项回升至 60.7%，总体低于有符号高度差的 2/28。结论是该奖励过于稀疏，保留作消融，默认仍使用 `signed_delta`、接近权重 5、抬升权重 40。

## 7. 训练课程

物体和物理难度同时采用由易到难的课程：

1. 每类 1 个简单物体，共 4 个；
2. 每类增加 2 个中等物体，共 12 个；
3. 加入每类 1 个困难物体，共 16 个；
4. 保持 16 个物体，逐渐加入质量、摩擦、物体方向和手部初始位置随机化。

每一阶段都同时覆盖四类物体，避免逐类别训练造成遗忘。固定条件成功率是必须完成的主结果，随机条件鲁棒性是第二阶段结果。

第一阶段执行选择（2026-07-18）：4 个代表物体各选 4 条训练轨迹，共 16 个环境。每组包含一条稳定成功锚点、两条该物体中抬升较高的失败轨迹和一条中等失败轨迹，并兼顾物体方向与尺度。具体编号冻结在 `custom_tools/configs/residual_stage1_trajectory_selection.yaml`；该课程只用于训练，模型仍在固定完整评估集上选择。

第一阶段首轮正式训练未达到扩大课程条件：最佳结果只在 mug 类成功 1/7，其余三类为 0；因此保留 4 个代表物体，先解决探索噪声和 reward 对齐问题。

扩大课程的依据（2026-07-18）：4物体最佳残差在课程内由4/40提高到5/40，但在课程外由8/120降到4/120，说明主要问题是小课程过拟合。下一组实验因此直接覆盖全部16个训练物体，每个物体使用2条PPO轨迹，共32个并行环境。轨迹从 `bc_multicategory_train` 对应的原始索引中选择，并避开固定评测索引0～9；与 `bc_multicategory_valid` 的重叠为0。2轮资源测试峰值预留显存912 MiB、无数值异常，证明本地可运行。

16物体训练结果（2026-07-19）：共享残差PPO完成150轮。严格同物体留出集上，零残差BC为2/100，第50/100/150轮残差依次为1/100、4/100、8/100；第150轮宏平均成功率8.82%、平均最大抬升5.27 cm，且四类均有成功，因此按预定规则选择第150轮。在固定索引0～9上，严格配对BC为13/160，残差为8/160；逐轨迹为保留1、新增7、丢失12，其中bowl丢失8条。

训练拟合诊断（2026-07-19）：冻结的32条PPO训练轨迹对应的源示范全部满足官方成功判据，但在完全相同的初始轨迹索引上，零残差BC仅成功1/32、平均最大抬升2.36 cm，第150轮残差PPO为0/32、1.28 cm，并丢失BC唯一的成功。因此低成功率不是因为选到了失败示范，而是BC在闭环仿真中难以复现成功动作；当前PPO也没有在自己的训练起点上形成稳定抓取。下一步应先诊断并增强BC闭环复现，再决定是否加入门控，不能把门控当作主要修复手段。

闭环误差诊断进一步表明，BC在冻结训练轨迹的专家观测上动作MAE仅0.0072，但在自身状态上增至0.2648，即36.8倍。分阶段放大倍数为接近13.4、预抓取46.0、闭合68.1、抬升71.3；手指误差在预抓取阶段首先明显增大。预处理文件共有70帧观测，但最后一个动作槽是未赋值的全零占位，正式诊断已按官方Dataset的规避意图只使用0～68帧。结论是继续降低离线loss预计收益有限，下一方法应直接提高偏离示范后的恢复能力。

门控残差阶段（2026-07-19）：以当前最佳噪声BC为冻结基础，在16个物体上各选2条BC成功锚点和2条有效失败，共64环境。Actor输出28维残差及腕部/手指两个0～1门；门初始为0.1，使训练开始时动作接近BC，随后按状态学习何时、改多少。与相同seed、轨迹、PPO和基础reward的无门残差对照比较；门控方法另加0.01的小门开启代价，并单独记录两门均值和波动。原固定索引0～9可能进入训练，因此后续只用独立100条留出集选模。

门控结果：独立留出集上，无门第50/100/150轮为11/9/8，门控为16/16/14，选择门控第50轮。纯噪声BC与门控第50轮均为16/100、宏平均18.71%，但BC平均最大抬升10.76 cm、失败率7.81%，优于门控的9.41 cm、8.59%，所以最终基础模型仍选噪声BC。门控相对BC保留12、新增4、丢失4；无门仅保留3、新增8、丢失13，证明门控显著缓解残差学习的负迁移，但尚未提高总成功率。

## 8. 评价协议

- 主要指标：未修改官方代码得到的 `successes` 成功率。
- 第一阶段每 50 轮在同 4 个物体的全部 28 条留出轨迹上确定性评价；验证轨迹不参与 PPO 更新。
- 连续 3 次验证没有改善则早停，最多训练 300 轮。
- 最终正式评价：每个物体 40 次，固定随机种子。
- 分别报告训练物体/未见物体、固定条件/随机条件。
- 第一阶段 checkpoint 先按 4 类宏平均官方成功率选择；成功率相同时依次比较平均最大抬升（差异至少 1 mm）和失败率，不按训练 reward 选择。
- 第一阶段结束后再运行 16 个训练物体各 10 条的固定评测，决定是否扩大课程；该评测不反向用于选择第一阶段 checkpoint。
- Isaac Gym 在同一进程内反复销毁训练仿真并创建16组验证仿真时可能触发CUDA非法访问；正式多物体评价改为每个物体一个独立Python/CUDA进程，再汇总相同官方指标。该工程处理不改变动作、轨迹或成功判据。
- 4 个未见物体不参与调参和选模，只在方法确定后进行最终测试。
- 辅助指标：相对初始高度的 5/15/30 cm 抬升率、最大高度、掉落率、飞出率和动作平滑度；这些指标只用于解释失败，不能替代官方成功率。

第一阶段 16 物体固定评测（2026-07-18）：接近权重 5 的 `best.pt` 在 160 条轨迹上成功 9 条（5.625%），低于相同轨迹的 warm-start BC 12 条（7.5%）。分类别 BC→残差为 bottle 1→2、mug 1→3、bowl 8→4、camera 2→0。4 个课程物体合计 4→5，其余 12 个物体 8→4；逐轨迹保留 2 个原成功、新增 7 个成功、丢失 10 个成功。结论是残差对训练课程有小幅改善，但共享策略在未参与 PPO 的物体上发生明显负迁移，当前 checkpoint 不能作为全物体最终模型。

全部正式试验只保存指标、物体 ID 和随机种子。最终根据固定评价结果，每类选择 1 个成功和 1 个代表性失败，共 8 条轨迹逐条重放并渲染，避免保存数百个视频占用磁盘和显存。

## 9. 实验优先级

性能优化补充（2026-07-19）：当前噪声BC的16/100只是第一轮基线，不视为性能上限。后续按“冻结全新未见测试集v2→验证批量评估与原逐物体口径一致→在原100条留出集搜索BC epoch及观测噪声±0.01/0.02/0.03/0.05→只对入选方法做多seed→最后仅一次打开v2测试集”的顺序进行。网络已是`1024-1024-512-512`的大MLP，且教师状态误差很小，因此先解决闭环分布偏移，不优先扩大网络。若噪声BC仍不足，再尝试带BC行为锚定损失的门控残差PPO，随后才是DAgger式偏离状态纠正和3帧时序BC。

噪声搜索阶段结果：逐物体严格评估重复3次后，±0.05 epoch40为19/18/20，±0.03 epoch100为16/16/18，±0.02 epoch100控制组为14/14/15。±0.05的平均宏成功率21.01%，比控制组16.89%提高4.12个百分点，且四类均稳定有成功；当前将其作为最佳checkpoint。下一步用2026/2027两个额外训练seed对±0.05 epoch40和±0.02 epoch100做配对验证，避免把单次训练随机性误判为方法收益。

多训练seed结果：seed2025/2026/2027下，±0.05 epoch40平均成功数分别为19.0/17.5/7.5，±0.02 epoch100为14.33/3.0/16.5。强噪声跨seed平均仍较高，但只在2/3 seed获胜，不能宣称普遍稳定提升；离线验证loss在同配置不同seed间几乎相同，说明闭环接触放大了离线指标无法体现的微小动作差异。为降低seed方差，下一低成本实验参考Model Soups/SWA，对同一官方初始化得到的优秀BC做权重平均；若平均模型不能严格提升，则进入行为锚定残差PPO。

Model Soup结果：将±0.05 seed2025/2026的epoch40权重按2:1平均，严格三轮为25/24/23，平均24/100、宏平均22.44%，高于最佳源模型19/100；失败率由10.79%降至8.73%，推理仍为单网络。等权双模型和三模型粗筛明显较差，因此不再搜索更多配比。新soup在bottle、mug、camera上提高而bowl下降，后续残差PPO必须以soup自己的训练集成功轨迹重建锚点课程，并继续按四类宏平均选模。

### 必须完成

1. 官方 BC 评价；
2. 官方权重微调与多物体从头 BC 的比较；
3. 最佳 BC + 单一共享有界残差 PPO；
4. 固定/随机条件和训练/未见物体评价；
5. reward、成功率曲线，8 个案例和 1～2 页报告。

### 短消融

- 1/3/5 帧历史；
- 分组残差与统一残差；
- 有无物理随机化。

### 时间充足再做

- 整个 BC 策略直接 PPO 微调；
- 类别残差专家生成成功轨迹，再蒸馏为统一学生策略；
- 将训练物体扩大到 20～30 个。

可选蒸馏方案借鉴专家到统一策略的思想，但不等同于传统 MoE。最终学生仍是一个共享网络，不保留多个专家参与推理。

## 10. 一周安排与止损条件

| 时间 | 工作 |
| --- | --- |
| 第 1 天 | 筛选 20 个物体，检查 mesh、轨迹、预处理和仿真加载 |
| 第 2 天 | 两种多物体 BC 训练、评价和基础模型选择 |
| 第 3 天 | 独立实现并跑通残差环境、reward、PPO和日志 |
| 第 4 天 | 短训练、历史消融和显存/速度测试 |
| 第 5 天 | 正式残差 PPO 长训练和物理随机化 |
| 第 6 天 | 最终评价、曲线与 8 个案例渲染 |
| 第 7 天 | 完成报告并保留排错时间 |

第 4 天结束时，残差 PPO 必须表现出官方成功率或抬升高度的明确改善；否则停止增加功能，集中诊断基础 BC、reward 和残差范围。只有第 5 天前核心实验已有可靠结果，才启动类别专家蒸馏。

## 11. 实施边界

- 官方源码和配置保持 Git 基准；自定义实现使用包装器、子类、新入口和配置副本。
- 训练前先在本地用少量环境实测显存和速度，再决定并行环境数和长训练时长。
- 服务器只在本地跑通且明确需要加速时使用，不能影响他人正在运行的任务。
- 每次实际操作继续在 `PROJECT_LOG.md` 中只记录操作、目的和结果。
